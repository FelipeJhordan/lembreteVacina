import styled from "styled-components";
import { Section } from "../../styles";

const NotFoundSection = styled(Section)`

`
const NotFoundHeadingMensage = styled.h1`
    color: #fce
`

export {
    NotFoundSection,
    NotFoundHeadingMensage
}